apiVersion: v1
kind: Service
metadata:
  name: sm-soc
  namespace: sm
  labels:
    app: sm-soc
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-soc
  ports:
  - name: http
    port: 8460
    targetPort: http
    protocol: TCP
